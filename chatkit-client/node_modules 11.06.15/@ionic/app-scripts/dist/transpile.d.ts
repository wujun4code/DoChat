import * as ts from 'typescript';
export declare function transpile(sourceText: string, sourcePath: string, compilerOptions: ts.CompilerOptions, reportDiagnostics: boolean): TransformedOutput;
export declare function resolveId(importee: string, importer: string, compilerOptions: ts.CompilerOptions): string;
export declare function getTsConfig(rootDir: string): TsConfig;
export declare function getCompilerOptions(rootDir: string): ts.CompilerOptions;
export interface TransformedOutput {
    code: string;
    map: any;
}
export interface TsConfig {
    compilerOptions: TsCompilerOptions;
    include: string[];
    exclude: string[];
}
export interface TsCompilerOptions {
    module: string;
    noEmitOnError: boolean;
    outDir: string;
    removeComments: boolean;
    target: string;
}
