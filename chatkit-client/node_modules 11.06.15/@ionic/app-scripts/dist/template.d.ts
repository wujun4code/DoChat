import { BuildContext, BuildOptions } from './util/interfaces';
export declare function templateUpdate(event: string, path: string, context: BuildContext, options: BuildOptions): Promise<boolean>;
export declare function inlineTemplate(sourceText: string, sourcePath: string): string;
export declare function replaceTemplateUrl(match: TemplateUrlMatch, templateContent: string): string;
export declare function getTemplateMatch(str: string): TemplateUrlMatch;
export interface TemplateUrlMatch {
    start: number;
    end: number;
    component: string;
    templateProperty: string;
    templateUrl: string;
}
